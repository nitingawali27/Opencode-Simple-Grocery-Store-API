# =============================================================================
# REQUIREMENTS.TXT - Python Dependencies
# =============================================================================
# This file lists all the Python packages needed to run the tests.
# 
# To install all dependencies, run:
#   pip install -r requirements.txt
#
# Each line specifies a package and optionally a version constraint:
#   package_name>=1.0.0  means "version 1.0.0 or higher"
#   package_name~=1.0.0  means "version 1.x.x where x>=0"
# =============================================================================

# HTTP library for making API requests
# Used by: utils/api_client.py
# Provides: requests.get(), requests.post(), etc.
requests>=2.28.0

# Testing framework
# Used by: All test files (test_*.py)
# Provides: pytest fixtures, assertions, test discovery
pytest>=7.0.0

# HTML report generation plugin for pytest
# Used by: conftest.py (generates HTML reports)
# Provides: --html command line option, beautiful test reports
pytest-html>=4.0.0

# Optional: Parallel test execution plugin
# Usage: pytest -n auto (runs tests in parallel)
# Provides: Faster test execution for large test suites
pytest-xdist>=3.0.0
